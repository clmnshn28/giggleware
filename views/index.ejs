<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>&lt;Celmin.Dev/&gt;</title>
        <link rel="stylesheet" href="/styles/style.css" />
    </head>
    <body>
        <header>
            <h1>Giggleware</h1>
            <h2>Two-Part Joke Generator</h2>
            <div class="category-container">
                <p class="category-name">Categories:</p>
                <% joke.forEach(j => {%>        
                    <p class="category-value"><%= j.category %></p>
                <%}); %>
            </div>

            <button onclick="location.reload()" class="reload-btn">
                <img src="/images/refresh-svgrepo-com.svg" alt="Reload">
            </button>
        </header>
        <main>
            <div class="left__container">
                <div class="inner-container left">
                    <p class="setup-joke left"><%= joke[0].setup %></p>
                    <p class="delivery-joke left"><%= joke[0].delivery %></p>
                </div>
              
            </div>
            <div class="right__container">
                <div class="inner-container right">
                    <p class="setup-joke right"><%= joke[1].setup %></p>
                    <p class="delivery-joke right"><%= joke[1].delivery %></p>
                </div>
            </div>
        </main>
        <footer>
            <p>&copy;<%= new Date().getFullYear() %> &lt;Celmin.Dev/&gt;. All rights reserved.</p>
        </footer>    
    </body>
</html>
